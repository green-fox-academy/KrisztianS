# - Write a function called `sum` that sum all the numbers
#   until the given parameter

numbers = [20, 10, 100]

def sums(numbers):
    summary = sum(numbers)
    return summary

print(sums(numbers))